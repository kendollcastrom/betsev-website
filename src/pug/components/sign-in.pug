div(id='modal1' class='modalmask')
	div(class='modalbox movedown')
		a(href="#close" title="Close" class='close')
			span(class="material-symbols-outlined") cancel
		div(class='sign-in__container')
			h3 Inicia Sesión
			form(class='sign-in__form')
				div(class='sign-in__email')
					label(for="email") Correo electrónico
					input(type="text" id="email" name="email" required)
				div(class='sign-in__password')
					label(for="password") Contraseña
					input(type="password" id="password" name="password" required)
				button(class='sign-in__btn' type="submit") Iniciar sesión
			a(href='restablecer-contraseña.html' class='') ¿Olvidaste tu contraseña?
			p(class='sign-in__form-register') ¿No tienes cuenta? 
				a(class="" href='registrarse.html') Registrate
